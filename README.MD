Tool to generate open-source-disclosure-file for minio repo.

Generates OSDF for gopackages and npm packages.

Instructions
------------

go get github.com/krishnasrinivas/osd

cd ~/$GOPATH/minio/minio

osd > /tmp/psdf.txt
